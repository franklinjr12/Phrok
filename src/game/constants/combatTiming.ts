/**
 * Minimum gap between the player's melee swings.
 *
 * Attack animations are spaced to finish inside this window so consecutive
 * swings never cut each other off (see `presentation/attackAnimations.ts`).
 */
export const PLAYER_MELEE_COOLDOWN_MS = 850;
